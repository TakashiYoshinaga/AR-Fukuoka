//今回作成したプロンプト編集のコードを下記に差し替え


        prompt =
          `下記の指示を実現するA-Frameのa-から始まる要素をコードに追加・修正。ただし下記の条件を満たすこと。\n`
          +`コード：\n<a-scene>\n`+elmContainer.innerHTML+`</a-scene>\n`
          +`条件:\n"""
          - a-camera,a-assets,a-lightは使用しない。
          - scriptを使用しない。
          - アニメーションの設定を要求された場合は<a-animation>ではなくanimationコンポーネントを使用。
          - 背景の設定を要求された場合は、backgroundコンポーネントではなく<a-sky>を使用。
          - 必ず<a-sky>はシーンに存在させる。
          - 結果はマークダウンのコードブロックに記述。\n"""\n`
          + `指示：\n"""\n`+prompt+`\n"""`;