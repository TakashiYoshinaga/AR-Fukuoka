//outputText.value="Waiting...";の直下に下記を追加


        //プロンプトをTexAreaから取得
        let prompt = document.getElementById('input').value;
        //promptがない場合
        if (!prompt){outputText.value="プロンプトを入力してください"; return;}
        //プロンプトをmessageArrayに追加
        messageArray[1]={role: "user", content: prompt};
