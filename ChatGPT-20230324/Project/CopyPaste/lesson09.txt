//作業①：SendMessage関数内のtext=data.choices[0].message.content;直下に下記を追加
     

          //ChatGPTからの回答をmessagesArrayに追加
          messageArray.push({role: "assistant", content: result});
          //messagesArrayの要素数が9を超えたら、1番目から2つのメッセージを削除
          if(messageArray.length>9){ 
            messageArray.splice(1,2);
          }
     
     
//作業②：SendMessage関数内のtext="No Response";の直下に下記を追加


          //ChatGPTからの回答がなければ、最後の質問を削除
          messageArray.pop();
